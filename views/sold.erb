<h1>hello these are your sold albums</h1>

<h3>Sold Records</h3>

<% if @sold_albums.any? %>
  <ul>
    <% @sold_albums.each do |album| %>
       <li><a href="/albums/sold/<%= album.id %>"><%= album.name %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are currently no records to display.</p>
<% end %>

<a href="/albums">Return to album list</a><br>